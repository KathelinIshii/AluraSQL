CREATE TABLE PRODUTOS (
ID_PRODUTOS VARCHAR(10) NOT NULL,
DESCRITOR VARCHAR(100) NULL,
SABOR VARCHAR(50) NULL,
TAMANHO VARCHAR(50) NULL,
EMBALAGEM VARCHAR(50) NULL,
PRECO_LISTA DECIMAL(30,2) NULL,
PRIMARY KEY(ID_PRODUTOS)
);

GO

CREATE TABLE VENDEDORES (
ID_VENDEDORES VARCHAR(5) NOT NULL,
NOME VARCHAR(100) NULL,
BAIRRO VARCHAR(50) NULL,
COMISSAO FLOAT NULL,
DATA_ASMISSAO DATE NULL,
FERIAS BIT NULL,
PRIMARY KEY(ID_VENDEDORES)
);

GO

CREATE TABLE CLIENTES (
CPF VARCHAR(11) NOT NULL,
NOME VARCHAR(100) NULL,
ENDERECO VARCHAR(150) NULL,
BAIRRO VARCHAR(50) NULL,
CIDADE VARCHAR(50) NULL,
ESTADO VARCHAR(50) NULL,
CEP VARCHAR(8) NULL,
DATA_NASCIMENTO DATE NULL,
IDADE INT NULL,
GENERO VARCHAR(1) NULL,
LIMITE_CREDITO FLOAT NULL,
VOLUME_COMPRA FLOAT NULL,
PRIMEIRA_COMPRA BIT,
PRIMARY KEY(CPF)
);

GO

CREATE TABLE TABELA_VENDAS (
ID_TABELA_VENDAS VARCHAR(5) NOT NULL,
DATA_VENDA DATE NULL,
CPF VARCHAR(11) NOT NULL,
ID_VENDEDORES VARCHAR(5) NOT NULL,
IMPOSTO FLOAT NULL, 
PRIMARY KEY(ID_TABELA_VENDAS)
);

GO

ALTER TABLE TABELA_VENDAS
ADD CONSTRAINT FK_CLIENTES
FOREIGN KEY(CPF) REFERENCES CLIENTES (CPF);
--CONSTRAINT, é uma restrição de dados

GO

ALTER TABLE TABELA_VENDAS
ADD CONSTRAINT FK_VENDEDORES
FOREIGN KEY(ID_VENDEDORES) REFERENCES VENDEDORES (ID_VENDEDORES);